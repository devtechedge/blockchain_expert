# Optional. Used only for a local LLM review step.
# The static analyzer, SWC retrieval, MCP tools, and HITL board run fully offline.
# Do not set chain RPC URLs. Argus never sends transactions or talks to a node.
XAI_API_KEY=
